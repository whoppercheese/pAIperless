summary: Analyze document structure dynamically
description: Uses Ollama to infer document nature and importance signals without fixed types.
lock: ''
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    doc_id:
      type: integer
    text:
      type: string
  required:
    - doc_id
    - text
