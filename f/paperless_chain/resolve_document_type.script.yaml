summary: Resolve document type from summary
description: >-
  LLM call to assign document type when Paperless has none preset.
  Generates a type from the summary and creates it in Paperless.
lock: ''
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    doc_id:
      type: integer
    summary:
      type: string
    document_language:
      type: string
      default: de
  required:
    - doc_id
    - summary
